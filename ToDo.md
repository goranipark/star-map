# 별자리 퍼즐 게임 — ToDo.md

## 0-0. 교육과정 위치 (2026-09-10 확인)

- 대상: **초등 4학년 2학기 과학 「밤하늘 관찰」 단원** (2022 개정 교육과정, 12차시)
- 단원 구성: 달의 모양과 표면 → 달의 위상 변화 → 태양계 구성 → **별과 별자리 → 북극성 주변의 별자리**
- 이 앱이 덮는 범위는 단원 후반부(별과 별자리 / 북극성 주변의 별자리)이며,
  **정규 과학 수업 시수로 그대로 사용 가능**하다. 창의적 체험활동으로 뺄 필요 없고,
  수행평가에 활용해도 무방하다.
- 2015 개정에서는 같은 내용이 5학년 「태양계와 별」이었으나 **2022 개정에서 4학년으로 내려왔다.**
  (2015 개정 기준 자료를 참고할 때 학년 표기에 주의)
- 교과서가 다루는 북극성 주변 별자리는 **북두칠성 · 작은곰자리 · 카시오페이아자리** 3개.
  → 우리 앱의 **스테이지 1~3이 정규 범위**, 스테이지 4~5(케페우스·용자리)는 심화.
- 확장 여지: 같은 단원의 **달의 위상 변화**를 미니게임으로 붙이면 단원 전체를 덮는 도구가 된다.

## 0. 플랫폼 추천 결론

| 판단 항목 | 결론 |
|---|---|
| 웹 vs 앱 | **웹(React)** 추천 |
| 서버/DB | **불필요** (localStorage만으로 충분) |
| 개발 환경 | Vite + React (단일 정적 사이트로 빌드) |

### 왜 웹(React)인가

- **배포 편의성**: 학급 대상 수업이므로 링크 하나(GitHub Pages 등)로 즉시 접속 가능해야 함. 앱스토어 심사·설치·업데이트 배포 과정이 없는 웹이 훨씬 유리함.
- **기기 다양성**: 학교 현장은 교실 PC, 크롬북, 아이패드, 안드로이드 태블릿 등 OS가 제각각인 경우가 많음. Flutter 앱으로 만들면 플랫폼별 빌드·배포가 추가로 필요하지만, 웹은 브라우저만 있으면 동일하게 동작함.
- **UI 복잡도**: 이 게임은 퍼즐판·이야기 슬라이드·도감·설정 등 화면 전환과 상태 관리가 여러 개 얽혀 있어 React의 컴포넌트/상태관리 구조가 유리함 (기존에 사용해온 "복잡한 UI는 React" 원칙과 일치).
- **네트워크 의존 최소화**: 학교 와이파이 환경이 불안정한 경우가 많은데, 웹 앱을 PWA(오프라인 캐싱) 형태로 만들면 최초 접속 이후 오프라인에서도 실행 가능 — 네이티브 앱 설치 없이도 유사한 경험 제공 가능.
- Flutter 앱이 유리한 경우는 카메라/AR, 푸시 알림, 앱스토어 노출이 꼭 필요할 때인데, 이번 게임(별 잇기 + 이야기 학습)은 해당 사항이 없음.

### 왜 서버/DB가 불필요한가

- 이 게임은 **개인 진행 기록(완성한 별자리 도감)** 만 저장하면 되고, 학급 간 순위·랭킹·공동 데이터 요구사항이 없음 (행성 합치기 게임과 달리 온라인 리더보드 요구사항이 없음).
- 따라서 **브라우저 localStorage**에 진행 상황(완성한 별자리 목록, 별자리별 재관람 가능 여부)을 저장하는 것으로 충분함.
- 추후 "우리 반 전체 진행 현황 공유" 같은 기능이 필요해지면 그때 Firebase 등 서버리스 DB 도입을 검토하면 됨 (지금 단계에서는 과도한 설계).

---

## 1. 기술 스택 확정

- **프레임워크**: React + Vite
- **그래픽**: SVG (별/연결선/별자리 궤적) — 좌표 기반 클릭/드래그 판정에 용이
- **스타일링**: CSS Modules 또는 Tailwind (design.md 토큰을 CSS 변수로 정의해 재사용)
- **상태 저장**: `localStorage` (진행 상황), 별도 서버·DB 없음
- **오디오**: HTML5 Audio API (BGM, 효과음)
- **배포**: 정적 빌드 후 GitHub Pages (기존 교사 커뮤니티 공유 방식과 동일)
- **오프라인 대응(선택)**: Vite PWA 플러그인으로 서비스워커 캐싱 추가 검토

---

## 2. 로컬 설치·실행 Setup 스크립트

### Windows용 (`setup.bat`)

```bat
@echo off
echo ==============================
echo  별자리 퍼즐 게임 - 로컬 개발환경 설정
echo ==============================

where node >nul 2>nul
if %errorlevel% neq 0 (
    echo [오류] Node.js가 설치되어 있지 않습니다. https://nodejs.org 에서 LTS 버전을 먼저 설치하세요.
    pause
    exit /b 1
)

if not exist package.json (
    echo [1/3] Vite + React 프로젝트 생성 중...
    call npm create vite@latest . -- --template react
)

echo [2/3] 의존성 설치 중...
call npm install

echo [3/3] 개발 서버 실행 중... (브라우저가 자동으로 열립니다)
call npm run dev

pause
```

### Mac/Linux용 (`setup.sh`)

```bash
#!/bin/bash
echo "=============================="
echo " 별자리 퍼즐 게임 - 로컬 개발환경 설정"
echo "=============================="

if ! command -v node &> /dev/null; then
    echo "[오류] Node.js가 설치되어 있지 않습니다. https://nodejs.org 에서 LTS 버전을 먼저 설치하세요."
    exit 1
fi

if [ ! -f package.json ]; then
    echo "[1/3] Vite + React 프로젝트 생성 중..."
    npm create vite@latest . -- --template react
fi

echo "[2/3] 의존성 설치 중..."
npm install

echo "[3/3] 개발 서버 실행 중..."
npm run dev
```

**사용법**
1. 빈 프로젝트 폴더에 위 스크립트를 저장 (`setup.bat` 또는 `setup.sh`)
2. Windows는 더블클릭 실행, Mac/Linux는 `chmod +x setup.sh && ./setup.sh`
3. 최초 실행 시 Vite React 템플릿이 자동 생성되고 의존성 설치 후 개발 서버가 열림 (기본 주소: `http://localhost:5173`)

---

## 3. 개발 체크리스트 (concept.md 기반)

### Phase 1 — 프로젝트 뼈대
- [x] Vite + React 프로젝트 생성 및 `setup.bat`/`setup.sh` 작성
- [x] design.md의 색상/폰트/간격 토큰을 `:root` CSS 변수로 정의 (`src/styles/tokens.css`, 폰트 파일 `public/fonts/`에 동봉)
- [x] 라우팅 없이 단일 페이지 내 화면 전환(퍼즐판 / 이야기 / 도감)을 상태값으로 관리하는 기본 구조 설계 (`src/App.jsx`, `src/game/screens.js`)

### Phase 2 — 데이터 구조
- [x] `constellations.json` 작성 — 별자리 5개의 실제 적경·적위 좌표와 연결선 입력 완료 (`src/data/constellations.json`). 신화 텍스트·삽화 경로는 필드만 비워 두고 Phase 5에서 채움. 좌표→화면 변환은 `src/game/projection.js`
- [x] localStorage 저장 스키마 설계 및 구현 (`src/game/progress.js`, 키 `polaris-star-map/progress/v1`)

### Phase 3 — 핵심 퍼즐 컴포넌트
- [x] 배경 별밭 — 시드 기반 고정 배치 (`src/game/starfield.js`, `src/game/sky.js`)
      · **배경 별은 시드 기반 고정 배치**로 만든다. Phase 4-B에서 아이가 배경 별을 이어
        자기 성좌를 만들고 저장하는데, 별 위치가 매번 달라지면 저장한 성좌를 복원할 수 없다.
- [x] `ConstellationBoard` 컴포넌트 — 별 배치, 탭·드래그로 선 연결, 정답 판정 (`src/components/ConstellationBoard.jsx`)
      · **두 가지 모드를 지원하도록 설계**한다.
        (1) 퍼즐 모드 — 정답 연결선과 대조해 판정 (스테이지 1~5)
        (2) 자유 연결 모드 — 정답 없이 이은 선을 그대로 유지 (Phase 4-B 나만의 성좌)
- [x] 오답/정답 피드백 애니메이션 적용 (정답선 draw-in, 오답선 0.4초 붉게 페이드아웃, 감점 없음)
- [x] 스테이지 클리어 → 골드 pulse 완성 연출 + 별자리 이름 → 이야기 화면 전환
- [x] 스테이지 시작 시 밤하늘 전체를 보여준 뒤 해당 별자리로 부드럽게 확대 (북극성은 항상 화면 안에 유지)
- [x] 완성한 별자리는 은은한 골드로 하늘에 남겨 하늘이 채워지는 것을 보여줌
- [x] 도움말 버튼 — 힌트 문구 + 정답 모양 3초간 미리 보기

### Phase 4 — 이야기 & 도감
- [x] `StorySlides` 컴포넌트 — 신화 텍스트 슬라이드 (`src/components/StorySlides.jsx`). 컷 표시 점·이전/다음, 삽화가 없으면 별자리 모양 그림이 자리를 대신함. 대본이 비어 있는 별자리는 "준비 중"으로 안내
- [x] 이야기 대본 5편 24컷 전부 입력 (Phase 5에서 완료)
- [x] `Almanac`(도감) 컴포넌트 — 원형 성좌판 + 성좌 카드 목록, 카드를 누르면 이야기 다시 보기 (`src/screens/AlmanacScreen.jsx`)
- [x] `ConstellationCard` 컴포넌트 — 칭호/이름/권능/별자리 그림 (`src/components/ConstellationCard.jsx`). Phase 4-B의 빈 카드 틀로 그대로 재사용
- [x] 진행 기록 지우기 (교실 공용 기기에서 다음 학생에게 넘길 때) — 확인 한 단계 거침
- [x] localStorage 연동 (`src/game/useProgress.js`) — 껐다 켜도 진행 상황 유지, 시작 화면에 "이어서 별 잇기" 표시

### Phase 4-B — 나만의 성좌 만들기 (도감 5장 완성 후 해금)

> 웹소설 성좌물의 문법(칭호 + 권능 + 설화)을 빌려 온 마무리 창작 활동.
> 이 문법은 사실 그리스 신화 별자리의 구조 그대로여서 학습 내용과 충돌하지 않는다.
> **별자리는 자연이 정한 것이 아니라 사람이 붙인 약속**이라는 단원 핵심 개념을 몸으로 익히는 활동.
>
> ⚠️ 톤 원칙: '코인 · 후원 · 채널' 같은 성좌물 클리셰는 넣지 않는다(사행성 뉘앙스 +
> 특정 작품 고유 용어 회피). **'칭호 · 권능 · 설화'** 세 단어로만 장르감을 낸다.

- [x] **무대**: 게임에서 쓰던 그 성도판 위에서, 배경 별까지 포함해 자유롭게 잇는다
      (별도 별밭을 새로 만들지 않는다 — "지금 보는 이 별들로 내가 별자리를 만든다"는 경험)
- [x] `StarWeaverScreen` — 세 걸음(별 잇기 → 카드 채우기 → 완성) (`src/screens/StarWeaverScreen.jsx`)
      · 퍼즐판을 `mode="free"`로 재사용. 이미 이은 선을 한 번 더 이으면 지워지고, 전체 지우기 버튼도 있다
- [x] **칭호**: 수식어 카드 24개를 네 묶음으로 나눠 고르게 함 (`src/game/epithets.js`). "골라 줘" 버튼 포함
- [x] **권능**: 한 줄 직접 입력 (45자)
- [x] **이름**: 직접 입력 (14자). **만든 사람**도 선택 입력 — 인쇄해서 교실에 붙일 때 누구 것인지 알 수 있게
- [x] 성좌 카드를 **PNG 그림으로 저장** (`src/game/cardImage.js`, 1080×1350 세로)
      · 화면을 캡처하지 않고 캔버스에 새로 그린다 — 브라우저가 달라도 같은 그림이 나온다
      · 서버가 없어 자동 공유는 불가 — 만든 성좌는 그 기기의 localStorage에만 저장된다
- [x] `progress.js`에 `myConstellations` 항목 추가 (한 기기당 최대 12개)
      (형식은 constellations.json의 별자리 한 칸과 동일: 별 목록 + 연결선 쌍 + 카드 텍스트)
- [x] 만든 성좌를 도감 마지막 칸에 자기 카드로 표시 + 하나씩 지우기. 5개를 다 깨기 전에는 잠금 안내만 보임

### Phase 5 — 콘텐츠 제작
- [x] 5개 별자리 좌표 데이터 입력 — Phase 2에서 완료 (작은곰자리·큰곰자리·카시오페이아·케페우스·용자리)
- [x] 신화 이야기 대본 5편 작성 완료 (초등 눈높이 각색, 총 24컷)
      · 작은곰 5 / 큰곰 5 / 카시오페이아 5 / 케페우스 4 / 용 5
      · 큰곰자리 편은 "헤라가 바다에 못 닿게 했다" → **북두칠성이 지지 않고 도는 이유(주극성)**로 끝맺어
        단원 내용과 이어지도록 각색했다
- [x] **성좌 카드 문구 5편** — 별자리별 `칭호 / 권능` 작성 완료. 모두 실제 신화에서 끌어왔다.
- [x] **칭호 수식어 카드 목록** 24개 작성 (`src/game/epithets.js`)
- [ ] **삽화 29컷** — 화풍은 '옛 성도(星圖) 판화풍'으로 확정. 생성 프롬프트는 `art-prompts.md`,
      그림 파일은 `public/art/`에 `<별자리id>-<컷번호>.png`로 넣으면 자동 반영된다(없으면 별자리 그림으로 대체).
      **프롬프트 전부 작성 완료.** 남은 일은 실제 그림을 뽑아 파일로 넣는 것뿐이다.
      · **신화 장면 24컷** — 별을 그리지 않는다(흩뿌린 별가루는 허용, 이어진 별 무리는 금지)
      · **별자리 컷 5컷** — 각 이야기의 맨 마지막 한 장. 옛 성도처럼 **실제 별 배치 위에
        곰·왕비·용의 몸을 겹쳐 그린** 그림. AI는 몸의 윤곽선만 그리고 별은 앱이 얹는다.
        `?tune=1` 화면에서 **별자리 참고 그림**을 내려받아 AI에게 함께 올리면 배치가 맞는다
- [x] BGM/효과음 삽입 — 외부 소스 대신 라이선스 제약이 없는 Web Audio 생성음 사용, 저장되는 음소거 UI 포함

### Phase 6 — 마감 및 배포
- [x] 반응형 1차 처리 (360px 모바일·16:9 화면 브라우저 검증 완료, 실물 태블릿은 파일럿에서 추가 확인)
- [ ] 실제 학급 파일럿 테스트, 난이도 조정
- [ ] GitHub Pages 실제 배포 (프로덕션 빌드 성공, 수동 배포 워크플로 준비 완료)
- [x] PWA 오프라인 캐싱 적용 — 앱 셸 선캐시, 방문 자산 런타임 캐시, 사용자 승인 업데이트
