# GPT-6 Astra 작업 목록

> 작성일: 2026-09-11  
> 대상 모델: `gpt-6-astra`  
> 원칙: 여러 시스템이 얽혀 있거나, 잘못된 결정의 재작업 비용이 크거나, 교육·천문·사용성에 대한 종합 판단이 필요한 일은 Astra가 맡는다.

## 검토 결론

### 개발 진행 — 2026-09-11

남은 개선사항 1~3 반영: 성좌 초안 자동 저장·복원 및 편집 중 업데이트 새로고침 보류, 마지막 정답 입력 시 완료 저장, Web Locks를 사용한 탭 간 저장 직렬화와 작품 변경 충돌 처리. Node 테스트 30개와 실제 두 탭 브라우저 회귀 검증 통과.

후속 개선사항 4~6 반영: 단일 활성 포인터 및 취소 처리, 12칸 만석에서 명시적 작품 교체, 빌드 자산 선캐시와 버전 고정/설치 실패 복구. 자동 테스트 28개 및 브라우저 입력·보관 한도·오프라인 회귀 검증을 추가했다. 실제 터치 기기 검증은 남아 있다.

사용자 검토 요청의 개선사항 1~3을 반영했다: 성좌 작성 단계 왕복 시 퍼즐판 유지, 저장 실패 결과 전달 및 재시도/PNG 안내, 저장 작품 좌표·필드·선 참조 검증과 완료 ID 중복 제거. Node 테스트 27개 및 `scripts/check-weaver.mjs` 브라우저 회귀 검증 통과. 입력 멀티터치 개선과 보관 한도 정책은 별도 작업이다.

권장 선행 순서의 첫 항목인 교육·천문·신화 검토를 시작했다. [1차 검토 기록](docs/content-audit.md)에 근거, 반영한 문구, 후속 검증을 기록했다.

- [x] 공식 교육청 자료에서 `[4과13-03]` 범위 확인 및 튜토리얼에 별의 정의 보완
- [x] 북두칠성/큰곰자리 구분, 북극성 찾기 방향·거리, 주극성 단정 수정
- [x] 신화 각색 안내와 실제 하늘 컷 구분, 기본 탐구/더 알아보기 표시
- [ ] 37개 별의 좌표·등급·고유명 외부 자료 대조
- [ ] 신화 전승 출처와 기존 삽화 정합성 검증
- [ ] 퍼즐 입력 상태 전이 및 회귀 테스트 감사

아래 검토 결론은 최초 작성 당시 기준이다. 현재 저장·좌표·데이터·카드 관련 자동 테스트도 있으므로, 다음 감사는 실제 코드를 기준으로 수행한다.

현재 프로젝트는 단순 프로토타입을 넘어 핵심 플레이 흐름이 완성된 상태다. 남은 위험은 기능 개수보다 다음 경계에 집중되어 있다.

- 574줄의 `ConstellationBoard.jsx`가 포인터 입력, 정답 판정, 자유 연결, 카메라, 확대 UI, 애니메이션, 완료 타이머를 함께 책임진다.
- `StorySlides.jsx`와 `cardImage.js`는 이미지 폴백·별자리 오버레이·내보내기 결과를 서로 다른 렌더링 방식으로 구현한다.
- 자동 테스트는 카메라 6건뿐이므로 저장, 입력, 화면 흐름, 이미지 생성의 회귀 방어가 부족하다.
- 반응형 CSS는 존재하지만 실제 모바일·태블릿·교실 화면에서 검증됐다는 증거는 없다.
- 29개 삽화 참조 중 실제 파일은 작은곰자리 5개뿐이며, 오디오와 오프라인 배포 정책도 미정이다.

## Astra가 직접 맡을 작업

### 1. 핵심 퍼즐판 아키텍처와 입력 모델 검토

- [ ] `ConstellationBoard.jsx`의 상태 전이와 책임을 도식화한다.
- [ ] 탭→탭, 드래그, 포인터 취소, 화면 밖 이동, 멀티터치, 빠른 연속 입력을 포함한 입력 상태 머신을 설계한다.
- [ ] 퍼즐 모드와 자유 연결 모드의 공통 로직/분기 경계를 재정의한다.
- [ ] 카메라 계산, 입력 판정, 게임 상태, 렌더링을 분리할지 결정한다.
- [ ] 타이머와 React 상태/ref 동기화에서 발생 가능한 레이스를 검토한다.
- [ ] 리팩터링이 필요하면 동작 보존 테스트를 먼저 설계하고 단계적으로 수행한다.

완료 조건: 입력 상태와 완료 판정의 불변 조건이 문서화되고, 구조 변경 전후의 회귀 테스트가 있으며, 실제 터치 기기에서 끊김·중복 연결·유령 선이 재현되지 않는다.

### 2. 전체 사용자 여정과 상태 일관성 감사

- [ ] 처음 시작, 이어하기, 전 스테이지 완료 후 재시작, 이야기 재관람, 도감 이동, 초기화를 하나의 상태 전이표로 검증한다.
- [ ] `currentId`, `upcoming`, `replaying`, `lastPlayed`, `completed` 조합에서 모순되는 화면이 생기는지 확인한다.
- [ ] 저장 실패·저장공간 부족·손상 데이터·이전 스키마를 포함한 복구 정책을 결정한다.
- [ ] 나만의 성좌 저장과 PNG 다운로드 사이에 데이터가 달라질 수 있는 경로를 검토한다.
- [ ] 사용자가 만든 데이터의 필드·좌표·선 참조를 더 엄격히 검증할지 결정한다.

완료 조건: 주요 상태 전이가 표 또는 테스트로 고정되고, 실패 시 사용자 데이터와 화면이 예측 가능한 상태로 복구된다.

### 3. 교육 내용·천문 데이터·신화 표현 검증

- [ ] 2022 개정 초등 과학 교육과정과 앱의 학습 목표가 정확히 맞는지 공식 자료로 재확인한다.
- [ ] 5개 별자리의 적경·적위, 등급, 통상적인 연결선, 한글 고유명과 설명을 신뢰할 수 있는 천문 자료로 검증한다.
- [ ] 북극성 찾기, 주극성, 계절/시간에 따른 회전 설명이 과도하게 단순화되거나 오해를 만들지 않는지 확인한다.
- [ ] 신화 각색에서 폭력성, 책임 귀속, 성 역할, 사실과 설화의 구분이 초등 4학년 수준에 적절한지 검토한다.
- [ ] 스테이지 1~3의 정규 학습 범위와 4~5의 심화 범위를 UI에서 어떻게 표현할지 결정한다.

완료 조건: 데이터와 문구별 근거가 기록되고, 수정이 필요한 항목은 정확한 대체 문구와 함께 Sol에 전달된다.

### 4. 실제 기기 UX·접근성 평가 설계

- [ ] 대상 기기 매트릭스를 정한다: 최소 휴대폰, 아이패드급 세로/가로 태블릿, 크롬북/노트북, 교실 프로젝터.
- [ ] 어린이 손가락 크기, 손바닥 오입력, 낮은 정밀도의 드래그를 고려해 별 선택 반경과 피드백을 평가한다.
- [ ] 키보드만으로 퍼즐을 수행할 수 있어야 하는지 제품 요구사항을 결정한다.
- [ ] 스크린 리더용 대체 학습 흐름과 SVG의 접근 가능한 설명 수준을 설계한다.
- [ ] 색 대비, 색에만 의존하는 정오답 표시, 애니메이션 민감도, 200% 확대를 점검한다.
- [ ] 실제 학생 파일럿에서 관찰할 지표와 교사용 체크리스트를 만든다.

완료 조건: 재현 가능한 QA 매트릭스, 심각도, 기대 결과, 증거 캡처 규칙이 마련되고 단순 CSS 수정 항목은 Sol 작업으로 분리된다.

### 5. 삽화의 시각 체계와 정합성 최종 판단

- [ ] `art-prompts.md`의 29개 프롬프트가 하나의 판화풍 시각 체계를 유지하는지 검토한다.
- [ ] 이미 생성된 작은곰자리 JPG 5컷의 품질, 연속성, 아동 적합성을 기준 샘플로 채택할지 결정한다.
- [ ] 각 마지막 성도 컷에서 인물/동물 윤곽과 실제 별 좌표 오버레이가 충돌하지 않도록 생성·튜닝 절차를 확정한다.
- [ ] 저작권, 생성물 출처 기록, 교실 배포에 필요한 표시 정책을 정한다.
- [ ] 대체 도형과 완성 삽화가 섞여도 이야기 경험이 깨지지 않는 점진적 배포 방식을 결정한다.

완료 조건: 승인 기준과 샘플이 확정되고, Sol이 파일 적용과 최적화를 기계적으로 수행할 수 있다.

### 6. PWA·오프라인·업데이트 전략

- [ ] 29개 삽화와 오디오를 전부 선캐시할지, 단계별/런타임 캐시할지 용량과 교실 네트워크를 기준으로 결정한다.
- [ ] 새 콘텐츠 배포 시 서비스 워커와 `localStorage` 버전의 호환 정책을 설계한다.
- [ ] 첫 접속 실패, 부분 캐시, 업데이트 중 연결 끊김, 저장공간 부족 시 행동을 정의한다.
- [ ] GitHub Pages의 경로 제약과 정적 자산 참조 방식을 함께 검토한다.
- [ ] 설치 유도 UI가 수업 흐름에 실제로 필요한지 판단한다.

완료 조건: 캐시 대상, 만료/교체, 버전 상승, 장애 복구가 문서화되어 Sol이 구현만 수행할 수 있다.

### 7. 출시 전 통합 리뷰와 우선순위 결정

- [ ] 전체 코드에서 데이터 손실, 핵심 흐름 중단, 브라우저 호환성 문제를 중심으로 출시 차단 이슈를 찾는다.
- [ ] 자동 테스트, 수동 QA, 파일럿 테스트 결과를 합쳐 출시 여부를 판단한다.
- [ ] 필수 출시 범위와 후속 범위를 나눈다. PWA와 스테이지 4~5가 첫 배포에 필수인지도 여기서 결정한다.
- [ ] 성능 예산을 정하고 저사양 학교 기기에서 별·필터·애니메이션 렌더링 비용을 점검한다.
- [ ] 발견한 수정 작업을 파일, 재현 절차, 완료 조건이 있는 Sol용 티켓으로 변환한다.

완료 조건: 출시 차단 이슈가 0개이거나 명시적으로 수용되며, 첫 배포 범위와 후속 백로그가 분리된다.

## Astra가 맡아야 하는 에스컬레이션 조건

아래 상황이 발생하면 Sol 작업을 멈추고 Astra로 넘긴다.

- 한 수정이 퍼즐 입력, 카메라, 진행 저장 중 둘 이상을 동시에 바꾼다.
- 실제 터치 기기에서 간헐적으로만 발생해 단위 테스트로 재현되지 않는다.
- 기존 `localStorage` 데이터를 마이그레이션하거나 삭제할 가능성이 있다.
- 서비스 워커 때문에 새 배포와 구버전 자산이 섞인다.
- 별자리 좌표나 교육 문구의 정확성에 판단이 필요하다.
- 접근성을 위해 기존 게임 조작 방식을 새로 설계해야 한다.
- `ConstellationBoard.jsx`, `StorySlides.jsx`, `cardImage.js` 사이의 표현 결과를 통일하려면 공통 렌더러 설계가 필요하다.

## 권장 선행 순서

1. 교육·천문·신화 내용 검증
2. 퍼즐 입력 및 상태 전이 감사
3. 실제 기기 UX·접근성 QA 설계와 1차 실행
4. 삽화 승인 기준 확정
5. PWA·배포 정책 결정
6. Sol 구현 결과에 대한 출시 전 통합 리뷰

## 모델 배분 근거

OpenAI의 공식 모델 설명에서 GPT-5.6 Sol은 복잡한 전문 작업을 수행하는 주력 모델로 소개되고, GPT-6 Astra는 가장 어려운 종단 간 작업과 복잡한 추론·코딩·컴퓨터 사용을 위한 최고 성능 모델로 소개된다. 따라서 이 문서는 단순한 “코딩 가능/불가능” 구분이 아니라, 명세가 닫힌 구현은 Sol에 맡기고 여러 하위 시스템과 제품 판단이 얽힌 작업은 Astra에 맡기는 방식으로 비용과 재작업 위험을 함께 줄이도록 구성했다.

- GPT-5.6 Sol: https://developers.openai.com/api/docs/models/gpt-5.6-sol
- GPT-6 Astra: https://developers.openai.com/api/docs/models/gpt-6-astra
