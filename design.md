# 별자리 퍼즐 게임 — Design.md

> 참고 앱: **Stellaris** (아마추어 별관측용 앱 디자인 템플릿, kombai.com 디자인 라이브러리 수록)
> "깊은 밤하늘의 정밀 관측기기"를 컨셉으로 한 다크 네이비 + 골드 단일 포인트 컬러 디자인 시스템

---

## 1. 원본(Stellaris) 디자인 요약

| 구분 | 특징 |
|---|---|
| 컨셉 | 천체 관측 앱 = "정밀 계기판" 느낌. 그림자 없이 헤어라인(1px 얇은 선) 경계만으로 구조를 표현 |
| 배경 | 거의 검정에 가까운 미드나잇 네이비 단색, 미세한 노이즈 텍스처 |
| 포인트 컬러 | 골드 단 하나. 밝은 별, 활성 상태, 중요 수치에만 제한적으로 사용 |
| 형태 언어 | 원형(성도판, 링, 점) ↔ 각진 사각형(데이터 카드)의 대비 |
| 타이포 | 얇은 굵기의 Inter(라이트 웨이트) + 좌표·수치는 모노스페이스 폰트 |
| 모션 | 은은한 회전, 반짝임(twinkle), 3D 틸트 — 화려함보다 "관측 장비가 살아있는" 느낌 |

이 원본은 **성인 대상 정밀 관측 도구**의 느낌이 강하므로, 아래에서 **초등학생 대상 학습 게임**에 맞게 대비를 낮추고 가독성·터치 편의성을 높이는 방향으로 재해석한다.

---

## 2. 색상 시스템

### 2-1. 원본 팔레트 (참고용)

| 토큰 | 색상 | 용도 |
|---|---|---|
| gold | `#C4A030` | 유일한 포인트 컬러 |
| sky | `#0A0C1A` | 배경 |
| field | `#1A1E30` | 카드/패널 |
| silver | `#B0B8C0` | 텍스트 |
| hairline | `rgba(176,184,192,0.18)` | 얇은 구분선 |
| red-safe | `#8A2020` | 야간모드(관측용 적색 모드) |

### 2-2. 우리 게임 적용 팔레트

초등학생 대상이므로 원본보다 **명도 대비를 살짝 높이고, 포인트 컬러를 조금 더 밝게** 조정한다 (장시간 응시해도 눈이 편하도록 배경은 유지하되, 상호작용 요소의 시인성을 강화).

| 토큰 | 색상 | 용도 |
|---|---|---|
| `--bg-sky` | `#0B0C1F` | 기본 배경 (원본 sky보다 살짝 밝혀 태블릿 화면에서도 별이 잘 보이게) |
| `--bg-field` | `#1C2038` | 카드, 패널, 버튼 배경 |
| `--bg-nebula` | `radial-gradient(circle at 50% 40%, rgba(60,50,120,0.35), transparent 70%)` | 배경 성운 텍스처 (원본의 노이즈 텍스처를 은하수 느낌으로 대체) |
| `--gold` | `#E8C158` | 포인트 컬러 — 정답 별, 완성된 연결선, 진행 배지 (원본보다 채도·명도를 올려 초등 저시력 학생도 식별 쉽게) |
| `--gold-soft` | `rgba(232,193,88,0.15)` | 골드 글로우, 은은한 배경 발광 |
| `--star-white` | `#F5F7FF` | 일반 별 (미배치 상태) |
| `--line-cyan` | `#7FD8E8` | 연결선 보조 색상 (별자리별 진행선을 골드/시안 두 가지로 구분해 색약 학생 배려) |
| `--text-primary` | `#E4E7F0` | 본문 텍스트 (원본 silver보다 밝게, 가독성 우선) |
| `--text-muted` | `rgba(228,231,240,0.6)` | 보조 설명 텍스트 |
| `--hairline` | `rgba(228,231,240,0.16)` | 카드 테두리, 구분선 |
| `--hairline-strong` | `rgba(228,231,240,0.30)` | 강조 구분선 (도감 헤더 등) |
| `--wrong-red` | `#E06666` | 오답 연결선 (원본 night-vision 레드를 오답 피드백용으로 용도 변경, 은은한 산화톤 대신 명확한 경고색 사용) |

> 원본의 "야간모드(적색 전환)"는 우리 게임 목적과 맞지 않아 **제외**하고, 같은 색조를 오답 피드백 색으로 재활용했다.

---

## 3. 타이포그래피

원본은 초경량 웨이트(300)의 Inter + 모노스페이스 조합인데, **초등학생 대상 한글 서비스이므로 원본의 "얇고 정밀한" 톤을 유지하되 한글 가독성이 좋은 라운드형 폰트로 대체**한다.

| 토큰 | 폰트 | 크기 | 용도 |
|---|---|---|---|
| `--font-display` | Baloo 2 (영문) / 배달의민족 도현체 (한글) | 32~40px | 별자리 이름, 스테이지 타이틀 |
| `--font-heading` | 위와 동일, Bold | 22~26px | 도감/메뉴 섹션 제목 |
| `--font-body` | Pretendard 또는 여기어때 잘난체 | 16~18px | 신화 이야기 본문 (초등 저학년 고려해 원본 13px보다 확대) |
| `--font-mono` | JetBrains Mono | 12~13px | 좌표/점수/카운트 등 수치 표기 (원본의 "관측 장비" 느낌 유지 요소) |
| `--font-eyebrow` | Pretendard SemiBold | 12px, letter-spacing 0.15em, 대문자/자간 강조 | "STAGE 01", "1/5 별자리 완성" 같은 라벨 |

- 원본의 자간(letter-spacing -0.02em, 타이트한 트래킹)은 한글에는 부적합하므로 **한글 텍스트는 자간 0, 라벨류만 자간을 넓혀 사용**한다.
- 오브젝트명(별자리 이름)에 쓰인 원본의 "이탤릭 처리" 컨벤션은 한글 이탤릭이 가독성을 해치므로 **컬러 강조(골드)로 대체**한다.

---

## 4. 여백 및 그리드 규칙

원본의 4px 기준 스케일을 그대로 채택 (모바일/태블릿 터치 UI에도 잘 맞는 체계).

| 토큰 | 값 |
|---|---|
| `--space-xs` | 4px |
| `--space-sm` | 8px |
| `--space-md` | 16px |
| `--space-lg` | 24px |
| `--space-xl` | 32px |
| `--space-xxl` | 48px |

- **터치 타깃 보정**: 원본은 PC 마우스 기준 설계이므로, 별 노드·버튼 등 탭 대상은 최소 `44×44px` 이상 확보 (초등 태블릿 터치 기준).
- **레이아웃 구조**: 원본의 "중앙 원형 성도판 + 사이드 패널" 12컬럼 그리드 구조를 계승하되, 태블릿 반응형에서는 사이드 패널을 성도판 하단으로 재배치.
- **카드 모서리**: 원본은 `radius: 0~2px`(거의 각짐)이지만, 초등 대상 UI는 각진 모서리가 딱딱한 인상을 주므로 **카드 radius를 8~12px로 확대**하여 부드럽게 조정. 단, 성도판·별·연결점 등 "원형 요소는 원본과 동일하게 완전한 원(`border-radius: 9999px`)" 유지.

---

## 5. 컴포넌트 스타일 가이드 (게임 요소별 적용)

| 원본 컴포넌트 | 우리 게임 요소로 재해석 |
|---|---|
| 성도판(planisphere) 원형 차트 | **별자리 퍼즐판** — 중앙에 북극성 고정, 배경은 `--bg-nebula` 그라디언트, 헤어라인 링 대신 은은한 골드 글로우 링으로 "현재 스테이지 범위" 표시 |
| 반짝이는 별(twinkle) | 배경 장식 별에 그대로 적용 (opacity 0.4↔1, 3s 주기) — 정답 별은 twinkle 대신 **완성 시 골드로 확 밝아지는 pulse 애니메이션**으로 차별화 |
| 헤어라인 테두리 카드 | 신화 이야기 슬라이드 카드, 도감 항목 카드 — 그림자 없이 `--hairline` 테두리 + `--bg-field` 배경 |
| pill 형태 상단 네비게이션 | 상단 진행 표시 바 ("2/5 별자리 완성") — `--bg-field` 반투명 + blur 효과 |
| outline 버튼(골드 테두리) | "다음 이야기 보기", "다시 도전" 등 주요 액션 버튼 — 원본처럼 테두리형 대신, 초등 대상은 **터치 유도를 위해 골드 배경의 채움형 버튼**으로 변경 (원본 대비 대비를 낮추는 대신 명확성 우선) |
| target-row(모노폰트 리스트) | 도감의 별자리 목록 — 완성 별자리는 골드 아이콘 + 이름, 미완성은 실루엣 회색 처리 |
| toggle 스위치 | 효과음 on/off, 도움말 표시 등 설정 스위치에 그대로 적용 |

---

## 6. 모션 가이드

원본의 절제된 모션 철학("화려함보다 살아있는 느낌")을 유지하되, 아동 대상이므로 **성취 순간에는 좀 더 명확한 리워드 모션**을 추가한다.

| 상황 | 모션 |
|---|---|
| 배경 별 | 3초 주기 twinkle (opacity 0.4↔1), 별마다 딜레이 랜덤 |
| 별 연결 성공 | 선이 0.3초간 그려지는 draw 애니메이션 + 짧은 차임벨 |
| 오답 연결 | 선이 빨갛게 표시 후 0.4초 페이드아웃 (경고음 최소화, 좌절감 방지) |
| 별자리 완성 | 전체 선이 골드로 동시에 밝아지는 pulse + 별자리 실루엣 페이드인 (원본의 "은은한 골드 글로우"보다 확실하게 화면 전체 톤이 밝아지는 축하 연출) |
| 스테이지 전환 | 성도판이 부드럽게 회전하며 다음 별자리 영역으로 포커스 이동 (원본의 azimuth 링 회전 모티프 차용, 속도는 더 느리고 부드럽게) |
| 상단 네비게이션 | 스크롤 시 숨김/노출 대신, 항상 고정 노출 (초등 대상은 진행 상황을 계속 보여주는 것이 안심 요소) |

---

## 7. 아이콘 및 도형 원칙

- 원본의 "원형(하늘) vs 각진 사각형(데이터)" 대비 원칙을 계승.
- 별자리 관련 그래픽(별, 연결선, 별자리 실루엣)은 **손그림 느낌의 인라인 SVG**로 제작 (원본 방식 계승), 획 두께는 원본보다 두껍게(1.5~2px) 하여 작은 화면에서도 또렷하게.
- UI 아이콘(설정, 소리, 뒤로가기 등)은 outline 스타일, 둥근 선 굵기로 통일 — 원본의 Tabler 아이콘처럼 얇은 선이 아니라 **초등 대상 UI에 맞게 스트로크를 굵게(2px 내외)** 조정.

---

## 8. Do's and Don'ts (초등 학습 게임 기준으로 조정)

**Do**
- 골드는 "정답/완성/성취"에만 쓴다 — 포인트 컬러의 희소성 원칙은 원본 그대로 유지
- 헤어라인 + 은은한 글로우로 입체감을 표현하고, 그림자는 쓰지 않는다
- 원형(하늘 요소)과 각진 카드(정보 요소)의 대비를 유지한다
- 수치(점수, 진행률)는 모노스페이스 폰트로 표기해 "관측 기록" 느낌을 살린다

**Don't**
- 원본처럼 대비를 지나치게 낮추지 않는다 (초등 저학년 가독성 우선)
- 텍스트 자간을 좁히지 않는다 (한글은 자간을 좁히면 가독성이 크게 떨어짐)
- 오답에 과도한 경고음·진동 등 좌절감을 주는 피드백을 넣지 않는다
- 별자리 이야기 슬라이드에 지나치게 많은 정보를 한 화면에 넣지 않는다 (한 컷당 1~2문장)
