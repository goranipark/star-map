# GPT-5.6 Sol 작업 목록

> 작성일: 2026-09-11  
> 대상 모델: `gpt-5.6-sol`  
> 원칙: 요구사항과 완료 조건이 명확하고, 변경 범위가 한정되어 있으며, 기존 설계를 뒤집지 않는 구현은 Sol이 맡는다.

## 현재 프로젝트 요약

- React 19 + Vite 기반 정적 웹 앱이다.
- 타이틀 → 별 잇기 퍼즐 → 이야기 → 도감의 핵심 흐름이 구현되어 있다.
- 작은곰자리, 큰곰자리, 카시오페이아자리, 케페우스자리, 용자리의 데이터와 이야기가 들어 있다.
- 진행 기록과 사용자가 만든 성좌는 `localStorage`에 저장된다.
- 나만의 성좌 만들기와 PNG 카드 다운로드가 구현되어 있다.
- `boardCamera.test.js`의 6개 테스트는 현재 통과한다.
- 실제 삽화는 필요한 29컷 중 작은곰자리 JPG 5컷만 존재한다. 나머지 24컷은 대체 도형으로 표시된다.
- BGM/효과음, 실제 기기 QA, 배포, PWA는 아직 완료되지 않았다.

## Sol에 맡길 작업

### 1. 테스트 기반 확대

- [x] `package.json`에 표준 `test` 스크립트를 추가한다.
- [x] `progress.js` 단위 테스트를 작성한다.
  - 손상된 저장값 복구
  - 중복 완료 방지
  - 존재하지 않는 별자리 ID 제거
  - 사용자 성좌 최대 12개 유지
  - 저장/삭제/초기화
- [x] `constellations.js` 단위 테스트를 작성한다.
  - 양방향 선 키가 동일한지
  - 다음 스테이지 계산
  - 데이터의 별 ID와 연결선 참조 무결성
- [x] `projection.js`, `starfield.js`, `cardImage.js`의 순수 함수에 회귀 테스트를 추가한다.
- [x] 테스트 명령과 결과를 `README.md` 또는 개발 문서에 기록한다.

완료 조건: 네트워크 없이 한 명령으로 테스트가 실행되고, 저장·데이터·좌표의 주요 실패 경로가 자동 검증된다.

### 2. 확정된 삽화의 입력·최적화

- [ ] 완성된 삽화 파일을 `public/art/<별자리id>-<컷번호>.<확장자>` 규칙으로 배치한다.
- [x] 29컷의 파일명과 슬라이드 참조를 대조한다. (5컷 발견, 원본이 없는 24컷은 누락 목록으로 기록)
- [x] 누락 파일을 검사하는 작은 검증 스크립트를 추가한다. (`npm run check:art`)
- [ ] 이미지 크기와 용량을 웹 배포에 맞게 일괄 최적화하되 원본은 별도 보존한다.
- [ ] 마지막 성도 컷 5개에서 실제 별자리 오버레이가 그림과 어긋나지 않는지 확인한다.

완료 조건: 29컷 모두 로딩되고 깨진 경로가 없으며, 각 이야기의 마지막 컷에 오버레이가 정상 표시된다.

주의: 그림의 화풍 선정, 신화 장면의 해석, 별자리 실루엣 정합성 판정은 `astra.md`의 선행 작업이다. Sol은 확정된 산출물의 적용과 기계적 검증을 맡는다.

### 3. 오디오 구현

- [x] 외부 음원 대신 라이선스 제약이 없는 Web Audio 생성음을 사용한다.
- [x] 별 연결 성공, 오답, 스테이지 완성에 효과음을 연결한다.
- [x] `progress.settings.sound`와 연동한 소리 켜기/끄기 UI를 추가한다.
- [x] 브라우저 자동 재생 제한을 고려해 최초 사용자 입력 후 BGM을 시작한다.
- [x] 화면 전환과 컴포넌트 해제 시 오디오와 타이머를 정리한다.

완료 조건: 음소거 상태가 저장되고, 중복 재생·화면 전환 후 잔류 재생·자동 재생 오류가 없다.

주의: 어떤 소리를 쓸지와 아동 대상 음량/피드백 원칙은 먼저 사람이 확정해야 한다.

### 4. 명확한 반응형·접근성 수정 티켓

- [ ] Astra 또는 실제 기기 QA에서 확정된 화면별 CSS 수정안을 반영한다.
- [x] 360px급 휴대폰과 16:9 화면의 레이아웃 회귀를 수정하고 브라우저에서 확인한다. (실물 태블릿 확인은 파일럿에서 수행)
- [x] 버튼의 `type="button"`, 포커스 표시, 비활성 상태, 명확한 레이블을 일관되게 정리한다.
- [x] `prefers-reduced-motion`에 맞춰 반짝임·확대·완성 애니메이션을 줄인다.
- [x] 좁은 화면과 확대 환경의 텍스트 넘침·스크롤 경로를 보강한다.

완료 조건: Astra가 만든 QA 매트릭스의 각 항목이 스크린샷과 함께 통과한다.

### 5. 빌드·정적 배포 정리

- [x] 테스트와 `npm run build`를 검증한다. (24개 테스트 통과, Vite 프로덕션 빌드 성공)
- [x] Vite 상대 `base` 설정을 확인하고 수동 실행 GitHub Pages 배포 워크플로를 추가한다.
- [ ] 새로고침, 정적 자산 경로, 한글 파일명 다운로드를 배포 URL에서 확인한다.
- [x] 배포 및 로컬 실행 절차를 `README.md`에 문서화한다.

완료 조건: 깨끗한 환경에서 재현 가능한 빌드가 생성되고 배포 URL에서 핵심 흐름이 작동한다.

주의: 현재 이 검토 환경에서는 전역 npm 경로가 깨져 `npm run build`가 시작되지 않았고, Vite 직접 실행도 상위 디렉터리 읽기 제한 때문에 설정 파일 로딩에 실패했다. 이를 곧바로 저장소 코드 결함으로 판단하지 말고 정상 환경에서 먼저 재검증한다.

### 6. Astra 결정 이후의 PWA 구현

- [x] 보수적인 캐시 범위와 사용자 승인 업데이트 정책으로 서비스 워커를 구현한다.
- [x] 앱 셸과 필수 폰트를 선캐시한다.
- [x] 대용량 삽화와 해시된 빌드 파일은 방문 시 런타임 캐시한다.
- [x] 새 버전 안내와 캐시 갱신 동작을 구현한다.
- [x] 서버를 종료한 뒤 오프라인 첫 재방문에서 앱 셸이 정상 실행되는지 테스트한다.
- [ ] 실제 두 번째 배포에서 업데이트 안내와 버전 교체를 최종 확인한다.

완료 조건: 최초 온라인 접속 후 교실 네트워크가 끊겨도 핵심 게임이 실행되고, 새 배포가 영구적으로 구버전 캐시에 갇히지 않는다.

## Sol 작업 운영 규칙

- 한 작업은 가능하면 한 기능 또는 한 파일군으로 제한한다.
- 기존 게임 규칙, 별자리 좌표, 저장 스키마, 화면 흐름을 임의로 바꾸지 않는다.
- `ConstellationBoard.jsx`, `StorySlides.jsx`, `cardImage.js`의 구조를 크게 바꿔야 한다면 중단하고 Astra 작업으로 넘긴다.
- 실제 기기에서만 재현되는 입력 문제, 저장 데이터 마이그레이션, 서비스 워커 업데이트 충돌은 Astra에 에스컬레이션한다.
- 기능 변경마다 관련 테스트 또는 재현 절차를 함께 남긴다.

## 권장 실행 순서

1. 테스트 스크립트와 데이터 무결성 검사
2. 확정 삽화 29컷 입력 및 최적화
3. 확정 오디오 구현
4. Astra QA 결과에 따른 반응형·접근성 수정
5. 정상 환경 빌드 및 정적 배포
6. Astra가 정한 정책에 따른 PWA 구현
